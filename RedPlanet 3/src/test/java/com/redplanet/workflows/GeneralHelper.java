/*package com.redplanet.workflows;



import org.apache.log4j.Logger;

import com.ctaf.accelerators.TestEngine;
import com.ctaf.support.ExcelReader;
import com.quickflix.workFlows.ContentHelper;

public class GeneralHelper extends TestEngine {
	
	Logger logger = Logger.getLogger(ContentHelper.class.getName());
	ExcelReader xlsrdr = new ExcelReader(configProps.getProperty("TestData"),
			configProps.getProperty("sheetName2"));

  public void () {
	  
	  
	  
  }
}
*/